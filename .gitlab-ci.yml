 stages:
   - test
   - apply
 test_job:
     stage: test
     tags:
        - runner1
     script:
        - ansible-playbook myplaybook.yaml --check -i hosts
      
 deploy_job:
     stage: apply
     tags:
        - runner1
     script:
        - ansible-playbook myplaybook.yaml -i hosts
